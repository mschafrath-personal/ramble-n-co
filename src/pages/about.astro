---
const brand = "Ramble & Co.";
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>About — {brand}</title>
  </head>

  <body>
    <main class="wrap">
      <header class="nav">
        <a class="logo" href={base}>{brand}</a>

        <nav class="links">
          <a href={`${base}about`}>About</a>
          <a href={`${base}services`}>Services</a>
          <a href={`${base}contact`}>Contact</a>
        </nav>
      </header>

      <section class="content">
        <h1>About</h1>

        <p>
          Ramble & Co. is a small consulting + creative studio. We partner with
          teams to bring clarity to complex work — from early problem framing to
          practical systems that help teams move faster with less friction.
        </p>

        <p>
          Typical engagements include lightweight strategy, UX and content
          structure, and operational tooling (templates, workflows, and
          enablement). The goal is simple: make decisions easier and execution
          calmer.
        </p>
      </section>

      <footer class="footer">
        <p>© {new Date().getFullYear()} {brand}</p>
      </footer>
    </main>
  </body>
</html>

<style>
  :global(body) {
    margin: 0;
    background: #0b0b0d;
    color: rgba(255, 255, 255, 0.92);
    font-family: ui-sans-serif, system-ui, -apple-system,
      BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial;
  }

  .wrap {
    max-width: 980px;
    margin: 0 auto;
    padding: 28px 20px 64px;
  }

  .nav {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 16px;
    padding: 10px 0 26px;
  }

  .logo {
    text-decoration: none;
    color: rgba(255, 255, 255, 0.92);
    font-weight: 650;
  }

  .links {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .links a {
    color: rgba(255, 255, 255, 0.7);
    text-decoration: none;
    font-size: 14px;
    padding-bottom: 2px;
    border-bottom: 1px solid transparent;
  }

  .links a:hover {
    color: rgba(255, 255, 255, 0.92);
    border-bottom-color: rgba(255, 255, 255, 0.25);
  }

  .content {
    padding: 46px 0 26px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  h1 {
    margin: 0 0 14px;
    font-size: 40px;
    letter-spacing: -0.03em;
  }

  p {
    max-width: 760px;
    line-height: 1.75;
    color: rgba(255, 255, 255, 0.72);
    font-size: 16px;
  }

  .footer {
    margin-top: 52px;
    padding-top: 18px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.56);
    font-size: 13px;
  }
</style>
